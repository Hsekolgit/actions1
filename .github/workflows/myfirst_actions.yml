name: firstworkflow

on: push

jobs: 
  my-first:
    name: myfirst workflow 
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v6
      - name: running job
        run: |
          echo "welcome to workflow"
          ls -lart
          cat README.md
        

      #- name: list file
       # run: ls

      #- name: read
       # run: cat README.md
